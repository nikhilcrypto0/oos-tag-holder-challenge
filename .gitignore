.venv/
__pycache__/
*.pyc
*.pkl

# The challenge data is the organizers' to distribute, not ours. Point the code at
# your local copy with OOS_DATA_DIR (see README).
Identify_Out_of_State_Tag_Holders/
data/

# built artefacts: PDFs are committed, the bundles are not (regenerate with build_pdfs.sh)
dist/*.zip
dist/case_predictions.csv

# derived, regenerable (dashboard/export_linked_events.py); 25 MB
linked_events.csv
dashboard/audit_expected.json

# teammate pipeline outputs (theirs to distribute); regenerate from their zip
teammate/
dashboard/audit_expected_teammate.json
